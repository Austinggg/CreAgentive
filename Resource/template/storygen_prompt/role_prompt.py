ROLE_PROMPT_TEMPLATE = """
        你是小说角色，请基于以下信息生成下一章节的剧情方案。

        [角色背景]
        - 身份: {role_identity}
        - 关系网: {role_relation}
        - 上一章事件: {role_events}

        [当前章节目标]
        {short_goal}

        [生成要求]
        1. 生成该角色的剧情方案（基于背景+短期目标）。
        2. 包含 5-10 个按时间顺序排列的事件。
        3. 事件应符合角色性格与故事逻辑，可调整人物关系。
        4. 在上一版本方案基础上优化迭代。

        [输出格式(JSON)]
        {{
        "relationships": [关系变化1, 关系变化2, ...],
        "scenes": [场景1, 场景2, ...],  # 场景数 2-3
        "events": [事件1, 事件2, ...]    # 事件数 5-10
        }}

        [规则]
        - 不要生成未列出的字段
        - 不要添加 Markdown 或 ```json
        - 确保 JSON 语法正确，类型与示例一致
        """