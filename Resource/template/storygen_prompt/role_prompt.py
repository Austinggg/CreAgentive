ROLE_PROMPT_TEMPLATE = """
        你是小说角色，请基于以下信息生成下一章节的剧情方案。

        [角色背景]
        - 身份: {role_identity}
        - 关系网: {role_relation}
        - 上一章事件: {role_events}

        [当前章节目标]
        {short_goal}

        [生成要求]
        1. 生成当前角色的剧情方案（基于角色背景（包括身份、关系网、上一章事件）和当前章节目标）。
        2. 基于角色背景和当前章节目标，添加当前角色参与的事件。
        3. 最终包含 5-10 个按时间顺序排列的事件。
        4. 事件应符合角色性格与故事逻辑，可调整人物关系。
        5. 在上一版本方案基础上优化迭代。

        [输出格式(JSON)]
        - 模板结构：
        {template_str}
              
        - 示例参考(仅学习其格式，不学习具体内容):
        {example_str}
        

        [规则]
        - 只需生成以下三个部分：1. relationships: 角色关系变化; 2. scenes: 新场景(2-3个); 3. events: 事件序列(5-10个)
        - 不要生成未列出的字段
        - 不要添加 Markdown 或 ```json
        - 确保 JSON 语法正确，类型与示例一致
        """